# /servis.nikolav.rs/.htaccess
<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is NOT already for /public, send it there
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
