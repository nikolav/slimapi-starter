# PHP Slim Web API + GraphQL

This is a lightweight PHP Slim web application providing both a REST-style API and a GraphQL endpoint.  
The project uses:

- Slim Framework
- Eloquent ORM
- GraphQL (webonyx)
- DotEnv configuration
- Custom middleware (CORS, etc.)
- Modular route and resolver loading
